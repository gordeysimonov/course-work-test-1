spring:
  application:
    name: CourseWorkSymonov
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: BroXiK22
    driverClassName: org.postgresql.Driver
logging:
  level:
    root: INFO
  pattern:
    file: "%d %-5level [%thread] %logger : %msg%n"
  file:
    name: log-file.log